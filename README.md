# Лабораторная работа №9. Работа с API и асинхронными запросами в React

Современное приложение для поиска фильмов с использованием OMDB API, созданное на React и Vite.

## 📸 Скриншоты приложения

### Главная страница с результатами поиска
<img width="1000" alt="Сетка фильмов с поиском" src="https://github.com/user-attachments/assets/d4e257f9-5798-46a3-b4eb-67461056acae" />

### Модальное окно с детальной информацией
<img width="499" alt="Детали фильма в модальном окне" src="https://github.com/user-attachments/assets/548924ce-fdd1-4c5e-a49a-5f967fa42c70" />

## ✨ Возможности

- 🔍 Поиск фильмов по названию через OMDB API
- 🎬 Просмотр детальной информации о фильме в модальном окне
- ⭐ Отображение рейтинга IMDb, года выпуска, жанра и других данных
- 📱 Адаптивный дизайн для всех устройств
- ⚡ Быстрая загрузка благодаря Vite
- 🎨 Современный UI с градиентным фоном

## 📚 Основные концепции React

### Что такое useEffect?
**useEffect** — это хук React для выполнения побочных эффектов в функциональных компонентах. Он запускается после рендера и может использоваться для:
- Загрузки данных с API
- Подписки на события
- Ручного изменения DOM

**Важно:** useEffect НЕ МОЖЕТ быть async функцией! Async функция возвращает Promise, а useEffect ожидает либо undefined, либо cleanup-функцию.

**Правильный подход:**
```jsx
useEffect(() => {
  async function loadData() {
    const response = await fetch(url);
    const data = await response.json();
    setData(data);
  }
  loadData();
}, []);
```

### Зависимости useEffect
Второй аргумент useEffect — массив зависимостей. Он определяет, когда эффект должен выполниться повторно:
- `[]` — эффект выполнится только один раз при монтировании
- `[value]` — эффект выполнится при изменении `value`
- Без массива — эффект выполняется после каждого рендера (избегайте этого!)

### Cleanup функция
useEffect может возвращать функцию очистки, которая выполнится при размонтировании компонента:
```jsx
useEffect(() => {
  const controller = new AbortController();
  
  fetch(url, { signal: controller.signal })
    .then(/* ... */);
  
  return () => {
    controller.abort(); // Отменяем запрос при размонтировании
  };
}, []);
```

### Работа с асинхронными запросами
**Fetch API** — встроенный способ делать HTTP-запросы в JavaScript:
```jsx
const response = await fetch(url);
const data = await response.json();
```

**Обработка ошибок:**
- Проверка `response.ok` для HTTP ошибок
- Try-catch для сетевых ошибок
- Проверка ответа API на наличие ошибок

## 🤖 Использование ИИ в разработке

**Какую часть кода помог написать ИИ:**
- Структуру компонентов (`App.jsx`, `SearchBar.jsx`, `MovieList.jsx`, `MovieCard.jsx`, `MovieModal.jsx`)
- Логику асинхронных запросов к OMDB API с обработкой ошибок
- Реализацию useEffect с AbortController для отмены запросов
- Модальное окно с детальной информацией о фильме
- Стилизацию с градиентами и анимациями
- Адаптивную верстку для мобильных устройств

ИИ помог разобраться с правильным использованием useEffect и обработкой состояний загрузки/ошибок.

## 💡 Что делает useEffect в моём приложении?

**В компоненте MovieModal:**

useEffect загружает детальную информацию о фильме при открытии модального окна. Вот что происходит:

1. **Создание AbortController** — для возможности отмены запроса
2. **Объявление async функции внутри** — `loadMovieDetails()`, которая делает запрос к API
3. **Fetch запрос** — получает полную информацию о фильме по его IMDb ID
4. **Обновление состояний** — `setDetails()`, `setLoading()`, `setError()`
5. **Cleanup функция** — отменяет запрос при закрытии модального окна

**Почему useEffect, а не просто fetch в компоненте?**
- Fetch в теле компонента вызовется при каждом рендере → бесконечный цикл
- useEffect с зависимостью `[movieId]` запускается только когда открывается новый фильм
- Cleanup функция предотвращает утечки памяти при быстром закрытии окна

## 📊 Какие состояния я использовал и зачем?

### В App.jsx (главный компонент):
```jsx
const [movies, setMovies] = useState([]);        // Массив найденных фильмов
const [selectedMovie, setSelectedMovie] = useState(null);  // ID выбранного фильма
const [loading, setLoading] = useState(false);   // Идёт ли загрузка
const [error, setError] = useState("");          // Сообщение об ошибке
```

**Зачем каждое состояние:**
- `movies` — хранит результаты поиска, чтобы отобразить сетку карточек
- `selectedMovie` — контролирует, какое модальное окно открыто (null = закрыто)
- `loading` — показывает спиннер во время запроса, блокирует повторный поиск
- `error` — выводит пользователю понятное сообщение об ошибке

### В MovieModal.jsx:
```jsx
const [details, setDetails] = useState(null);    // Детальная информация о фильме
const [loading, setLoading] = useState(true);    // Загрузка деталей
const [error, setError] = useState(null);        // Ошибка загрузки деталей
```

**Почему отдельные состояния в модалке:**
- Модалка делает свой независимый запрос (другой endpoint API)
- Нужен свой индикатор загрузки внутри модального окна
- Ошибки могут быть разные: в поиске и в деталях

### В SearchBar.jsx:
```jsx
const [query, setQuery] = useState("");          // Текст поискового запроса
```

**Контролируемый input:**
- Связываем `value={query}` и `onChange={(e) => setQuery(e.target.value)}`
- Позволяет валидировать ввод, очищать поле, блокировать кнопку

## 🤔 Где ИИ помог, а где пришлось разбираться самому?

### ИИ помог:
✅ Структура проекта — подсказал разбить на компоненты `SearchBar`, `MovieList`, `MovieCard`, `MovieModal`  
✅ Базовая логика fetch — шаблон с try-catch и обработкой ошибок  
✅ CSS стили — градиенты, сетка, модальное окно с overlay  
✅ AbortController — впервые узнал о нём от ИИ, он объяснил зачем нужен  

### Разбирался сам:
❌ **Почему useEffect не может быть async** — ИИ дал код, но не объяснил причину. Пришлось читать документацию React, чтобы понять про cleanup функцию  
❌ **Документация OMDB API** — ИИ не знал точные параметры API. Пришлось самому читать:
  - `s=` для поиска списка фильмов
  - `i=` для получения деталей по ID
  - `plot=full` для полного описания сюжета
  - Проверка `Response === "False"` для обработки ошибок API  
❌ **Условный рендеринг** — ИИ дал 4 блока (loading, error, data, empty), но логику их приоритета я расписывал сам:
```jsx
{loading && <Spinner />}
{error && !loading && <Error />}
{!loading && !error && movies.length > 0 && <MovieList />}
{!loading && !error && movies.length === 0 && <EmptyState />}
```
❌ **Закрытие модалки по клику на overlay** — ИИ дал код `onClick={onClose}`, но это закрывало окно при клике ВНУТРИ модалки. Пришлось разобраться с `e.target === e.currentTarget`

## 📖 Что из документации API было важнее всего?

### OMDB API — ключевые моменты:

1. **Два разных endpoint'а:**
   - `?s=query` — поиск фильмов (возвращает массив)
   - `?i=imdbID` — детали фильма (возвращает объект)

2. **Структура ответа при ошибке:**
```json
{
  "Response": "False",
  "Error": "Movie not found!"
}
```
Это важно! Без проверки `Response === "False"` приложение падало с ошибкой.

3. **Поля для деталей:**
   - `plot=full` — полный сюжет вместо короткого
   - `Poster` может быть "N/A" — нужен fallback
   - `imdbRating`, `Runtime`, `Genre` — не всегда есть

4. **Лимиты бесплатного ключа:**
   - 1000 запросов в день
   - Без лимита в секунду (но лучше не спамить)

5. **Что меня удивило:**
   - API возвращает `200 OK` даже при ошибке! Поэтому `res.ok` не достаточно, нужно проверять `data.Response`
   - Нет пагинации в поиске — возвращает максимум 10 фильмов
   - Поля как `BoxOffice`, `Awards` могут быть "N/A" или отсутствовать

**Без документации не обойтись** — ИИ может ошибаться в деталях API, поэтому всегда лучше проверять официальные доки.

## 🚀 Запуск проекта

### Получение API ключа:
1. Перейдите на [OMDB API](https://www.omdbapi.com/apikey.aspx)
2. Зарегистрируйтесь и получите бесплатный API ключ
3. Замените `API_KEY` в `App.jsx` на ваш ключ

### Установка зависимостей:
```bash
npm install
```

### Запуск в режиме разработки:
```bash
npm run dev
```

### Сборка для продакшена:
```bash
npm run build
```

## 🛠️ Технологии

- **React** — библиотека для создания пользовательских интерфейсов
- **Vite** — быстрый инструмент сборки
- **OMDB API** — база данных фильмов с IMDb рейтингами
- **CSS3** — анимации, градиенты и адаптивная верстка
- **Fetch API** — для HTTP запросов

## 📂 Структура проекта
```
src/
├── App.jsx              # Главный компонент (управление состоянием)
├── App.css              # Стили приложения
└── components/
    ├── SearchBar.jsx    # Поисковая строка
    ├── MovieList.jsx    # Список фильмов (сетка)
    ├── MovieCard.jsx    # Карточка фильма
    └── MovieModal.jsx   # Модальное окно с деталями
```

## 🎯 Ключевые моменты реализации

### Обработка состояний загрузки
```jsx
const [loading, setLoading] = useState(false);
const [error, setError] = useState("");
const [movies, setMovies] = useState([]);

// При запросе
setLoading(true);
setError("");

// При успехе
setMovies(data.Search);
setLoading(false);

// При ошибке
setError(err.message);
setLoading(false);
```

### Отмена запросов при размонтировании
```jsx
useEffect(() => {
  const abortController = new AbortController();
  
  fetch(url, { signal: abortController.signal })
    .then(/* ... */);
  
  return () => abortController.abort();
}, [movieId]);
```

### Модальное окно
- Клик по overlay закрывает окно
- Кнопка ESC закрывает окно (можно добавить)
- Загрузка деталей происходит внутри модального окна
- Отдельный запрос для получения полной информации о фильме

## 👨‍💻 Автор

Создано в рамках лабораторной работы №9 по изучению работы с API в React

## 📝 Лицензия

MIT
